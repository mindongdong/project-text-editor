# Project Text Editor - Phase 1 MVP

Editor.js 기반 프로젝트 게시물 작성 시스템 - Phase 1 Foundation & MVP

## 📋 Phase 1 완료 사항

### ✅ 구현된 기능

1. **프로젝트 초기 설정**
   - Next.js 15 + TypeScript 5 + Tailwind CSS 3
   - ESLint + Prettier 설정
   - 프로젝트 폴더 구조 설정

2. **타입 시스템**
   - TypeScript 타입 정의 (`src/types/editor.ts`)
   - Zod 스키마 검증 (`src/schemas/project.schema.ts`)

3. **핵심 컴포넌트**
   - **EditorComponent**: Editor.js 통합 컴포넌트
     - Paragraph 블록 지원
     - Header 블록 지원 (h1-h4)
     - useImperativeHandle을 통한 save() 메서드 노출
     - onChange 콜백 지원

   - **SimpleMetadataForm**: 제목 입력 폼
     - React Hook Form 통합
     - Zod 검증
     - 에러 메시지 표시

   - **ProjectViewer**: 콘텐츠 렌더링 컴포넌트
     - Paragraph/Header 블록 수동 렌더링
     - 기본 HTML sanitization

4. **라우팅**
   - `/` - 홈페이지
   - `/projects/new` - 프로젝트 작성 페이지
   - `/projects/[id]` - 프로젝트 조회 페이지

5. **데이터 흐름 (Phase 1 MVP)**
   - 작성 → 콘솔 로그 출력
   - localStorage에 임시 저장
   - 조회 페이지에서 localStorage 데이터 표시

## 🚀 시작하기

### 설치

```bash
npm install
```

### 개발 서버 실행

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속

### TypeScript 타입 체크

```bash
npm run type-check
```

### 빌드

```bash
npm run build
npm start
```

## 📁 프로젝트 구조

```
project-text-editor/
├── src/
│   ├── app/
│   │   ├── layout.tsx          # 전역 레이아웃
│   │   ├── page.tsx            # 홈페이지
│   │   ├── globals.css         # 전역 스타일
│   │   └── projects/
│   │       ├── new/
│   │       │   └── page.tsx    # 프로젝트 작성 페이지
│   │       └── [id]/
│   │           └── page.tsx    # 프로젝트 조회 페이지
│   ├── components/
│   │   ├── editor/
│   │   │   └── EditorComponent.tsx
│   │   ├── forms/
│   │   │   └── SimpleMetadataForm.tsx
│   │   └── viewer/
│   │       └── ProjectViewer.tsx
│   ├── types/
│   │   └── editor.ts           # TypeScript 타입 정의
│   ├── schemas/
│   │   └── project.schema.ts   # Zod 검증 스키마
│   ├── hooks/                  # 커스텀 훅 (Phase 3에서 추가)
│   └── utils/                  # 유틸리티 함수
├── public/                     # 정적 파일
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.js
```

## 🧪 테스트 방법

### 기본 플로우 테스트

1. **프로젝트 생성**
   - http://localhost:3000 접속
   - "새 프로젝트 작성" 버튼 클릭
   - 제목 입력 (예: "테스트 프로젝트")
   - 본문에 내용 작성 (Tab 또는 /로 블록 추가)
   - "저장" 버튼 클릭

2. **데이터 확인**
   - 브라우저 개발자 도구 열기 (F12)
   - Console 탭에서 JSON 데이터 확인
   - alert 팝업 확인

3. **프로젝트 조회**
   - 자동으로 조회 페이지로 이동
   - 저장한 내용이 표시되는지 확인

### TypeScript 컴파일 확인

```bash
npm run type-check
```

✅ 모든 타입 에러가 해결되어야 합니다.

## 📦 설치된 패키지

### Core Dependencies
- `@editorjs/editorjs` ^2.31.0 - Editor.js 코어
- `@editorjs/header` ^2.8.8 - Header 블록
- `@editorjs/paragraph` ^2.11.7 - Paragraph 블록
- `react-hook-form` ^7.65.0 - 폼 상태 관리
- `@hookform/resolvers` ^5.2.2 - Zod 통합
- `zod` ^4.1.12 - 스키마 검증
- `@tailwindcss/typography` ^0.5.19 - 타이포그래피 플러그인

### Framework
- `next` 15.0.3
- `react` ^18.3.1
- `typescript` ^5

## 🎯 Phase 1 완료 기준

✅ Editor.js가 정상적으로 초기화되고 텍스트 입력이 가능함
✅ 제목과 본문 내용을 저장하고 조회할 수 있음
✅ TypeScript 컴파일 에러가 없음
✅ 기본 라우팅이 동작함
✅ 코드 리뷰 준비 완료

## 🔜 다음 단계 (Phase 2)

- [ ] 이미지 업로드 기능 (`/api/upload-image`)
- [ ] 영상 임베드 (YouTube, Vimeo)
- [ ] 리스트 블록 (ordered, unordered)
- [ ] 전체 메타데이터 폼 (썸네일, 해시태그, 요약)
- [ ] 실제 API 연동

## 📝 주요 참고 사항

### Phase 1 제약사항
- **데이터 저장**: localStorage (임시)
- **지원 블록**: Paragraph, Header만
- **메타데이터**: 제목만
- **보안**: 기본 sanitization (DOMPurify는 Phase 4)

### 개발 팁
- Editor.js는 클라이언트 전용: `dynamic import` 사용
- `useEffect` 의존성 배열 주의
- 에디터 cleanup을 위한 `destroy()` 호출 필수

## 🐛 알려진 이슈

현재 알려진 이슈 없음. 발견 시 이슈 등록 바랍니다.

## 📄 라이선스

이 프로젝트는 MIT 라이선스를 따릅니다.
